'use client';
import { useState } from 'react';import { useRouter } from 'next/navigation';
export default function UserForm({user}){const r=useRouter();const [form,setForm]=useState(user||{name:'',email:'',password:'12345678',role:'reader',status:'active'});function u(e){setForm({...form,[e.target.name]:e.target.value})}async function submit(e){e.preventDefault();const url=user?`/api/users/${user._id}`:'/api/users';const method=user?'PUT':'POST';const res=await fetch(url,{method,headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await res.json();if(!res.ok)return alert(d.error||'Failed');r.push('/dashboard/users');r.refresh()}return <form onSubmit={submit} className="card p-6 grid gap-4"><input className="input" name="name" placeholder="Name" value={form.name} onChange={u}/><input className="input" name="email" placeholder="Email" value={form.email} onChange={u}/><input className="input" name="password" placeholder="Password (leave blank when editing to keep old)" value={form.password||''} onChange={u}/><select className="input" name="role" value={form.role} onChange={u}><option value="admin">Admin</option><option value="writer">Writer</option><option value="reader">Reader</option></select><select className="input" name="status" value={form.status} onChange={u}><option value="active">Active</option><option value="blocked">Blocked</option></select><button className="btn-primary">Save User</button></form>}
