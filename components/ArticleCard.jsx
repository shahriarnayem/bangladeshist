import Link from 'next/link';import { fmtDate, readTime } from '@/lib/utils';
export default function ArticleCard({article,big=false}){return <Link href={`/article/${article.slug}`} className={`card overflow-hidden block hover:border-red-900 transition ${big?'md:grid md:grid-cols-2':''}`}><img src={article.coverImage||'/placeholder.svg'} className="w-full h-52 object-cover"/><div className="p-5"><span className="badge">{article.category}</span><h2 className={`${big?'text-3xl':'text-lg'} font-black mt-4 leading-tight`}>{article.title}</h2><p className="muted mt-3 line-clamp-2">{article.excerpt}</p><p className="muted text-xs mt-4">{article.author || 'admin'} · {fmtDate(article.createdAt)} · {readTime(article.content)} min read</p></div></Link>}
