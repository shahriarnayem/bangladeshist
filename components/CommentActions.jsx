'use client';
import { useRouter } from 'next/navigation';
export default function CommentActions({id,status}){const r=useRouter();async function update(status){const res=await fetch(`/api/comments/${id}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})});if(!res.ok)return alert('Failed');r.refresh()}async function del(){if(!confirm('Delete comment?'))return;const res=await fetch(`/api/comments/${id}`,{method:'DELETE'});if(!res.ok)return alert('Failed');r.refresh()}return <div className="flex gap-3"><button onClick={()=>update('approved')} className="text-red-500">Approve</button><button onClick={()=>update('pending')} className="text-red-500">Pending</button><button onClick={()=>update('rejected')} className="text-red-500">Reject</button><button onClick={del} className="text-red-500">Delete</button></div>}
