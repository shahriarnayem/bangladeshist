'use client';
import { useState } from 'react';import { useRouter } from 'next/navigation';import Link from 'next/link';
export default function Login(){const r=useRouter();const [email,setEmail]=useState('writer@example.com');const [password,setPassword]=useState('12345678');const [loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setLoading(true);const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await res.json();setLoading(false);if(!res.ok)return alert(d.error||'Login failed');r.push('/dashboard');r.refresh()}return <main className="container py-16"><div className="card p-8 max-w-3xl mx-auto"><h1 className="text-3xl font-black">Reader / Writer Login</h1><p className="muted mt-2">Seed: writer@example.com / 12345678</p><form onSubmit={submit} className="grid gap-4 mt-8"><input className="input" value={email} onChange={e=>setEmail(e.target.value)} type="email"/><input className="input" value={password} onChange={e=>setPassword(e.target.value)} type="password"/><button className="btn-primary">{loading?'Logging in...':'Login'}</button></form><p className="muted mt-5">No account? <Link className="text-red-500" href="/register">Register</Link> · <Link className="text-red-500" href="/forgot-password">Forgot password?</Link></p></div></main>}
