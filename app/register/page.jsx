'use client';
import { useState } from 'react';import { useRouter } from 'next/navigation';
export default function Register(){const r=useRouter();const [form,setForm]=useState({name:'',email:'',password:''});function u(e){setForm({...form,[e.target.name]:e.target.value})}async function submit(e){e.preventDefault();const res=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const d=await res.json();if(!res.ok)return alert(d.error||'Failed');alert('Account created');r.push('/login')}return <main className="container py-16"><div className="card p-8 max-w-3xl mx-auto"><h1 className="text-3xl font-black">Create Reader Account</h1><form onSubmit={submit} className="grid gap-4 mt-8"><input className="input" name="name" placeholder="Full name" onChange={u} required/><input className="input" name="email" placeholder="Email" type="email" onChange={u} required/><input className="input" name="password" placeholder="Password min 8 characters" type="password" onChange={u} required/><button className="btn-primary">Create Account</button></form></div></main>}
