'use client';
import { useState } from 'react';
export default function Forgot(){const [email,setEmail]=useState('');const [token,setToken]=useState('');async function submit(e){e.preventDefault();const res=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await res.json();if(!res.ok)return alert(d.error||'Failed');setToken(d.resetToken)}return <main className="container py-16"><div className="card p-8 max-w-3xl mx-auto"><h1 className="text-3xl font-black">Forgot Password</h1><p className="muted mt-2">Development mode returns reset token on screen.</p><form onSubmit={submit} className="grid gap-4 mt-8"><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><button className="btn-primary">Create Reset Token</button></form>{token&&<div className="alert alert-success mt-5">Reset link: <a className="text-red-500" href={`/reset-password?token=${token}`}>/reset-password?token={token}</a></div>}</div></main>}
