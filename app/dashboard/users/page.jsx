import Link from 'next/link';import { getSession } from '@/lib/session';import { redirect } from 'next/navigation';import { getDb } from '@/lib/mongodb';import UserActions from '@/components/UserActions';
export const dynamic='force-dynamic';
export default async function Users(){const s=await getSession();if(s.role!=='admin')redirect('/dashboard');const db=await getDb();const users=await db.collection('users').find({}).sort({createdAt:-1}).toArray();return <div><div className="flex justify-between items-center mb-8"><h1 className="text-3xl font-black">Users</h1><Link href="/dashboard/users/new" className="btn-primary">Add User</Link></div><div className="card overflow-hidden"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th><th>Actions</th></tr></thead><tbody>{users.map(u=><tr key={u._id.toString()}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.status}</td><td><UserActions id={u._id.toString()}/></td></tr>)}</tbody></table></div></div>}
